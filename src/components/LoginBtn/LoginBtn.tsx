import './LoginBtn.scss';

import React from 'react';

function LoginBtn() {
  return (
    <button className="login-btn" type="button" aria-label="register">
      <span className="login-btn__icon" />
    </button>
  );
}

export default LoginBtn;
