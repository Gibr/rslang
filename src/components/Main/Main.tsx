import React from 'react';
import Router from '../Router/Router';
import './Main.scss';

function Main(): JSX.Element {
  return (
    <main>
      <Router />
    </main>
  );
}

export default Main;
